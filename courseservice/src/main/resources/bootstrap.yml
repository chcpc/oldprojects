spring:
  application:
    name: course
  cloud:
    config:
      discovery:
        enabled: true
        service-id: config
      profile: dev
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka,http://localhost:8762/eureka
mybatis:
  configuration:
    map-underscore-to-camel-case: true